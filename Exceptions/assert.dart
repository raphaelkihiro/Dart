/**
assert is a debugging tool in Dart.
It’s used to check if a condition is true at runtime,
and if it’s not, the program throws an AssertionError.
 */
 void main() {
  var age = 10;
  assert(age >= 18)
  print{"Access denied"}  
}
